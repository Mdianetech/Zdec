rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires très permissives pour les tests
    match /{document=**} {
      allow read, write: if true;
    }
  }
}